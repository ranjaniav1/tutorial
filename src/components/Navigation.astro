---
import { getEntry } from "astro:content";

import "../styles/nav.css";

const nav = await getEntry("navigation", "config");

console.log("nav", nav);

const data = nav?.data;
---

<h1>
    {data?.logo}
</h1>

<header class="navbar">
    <div class="container navbar-wrapper">
        <!-- logo -->

        <a href="/" class="logo">
            <span class="logo-text">
                {data?.logo}
            </span>
        </a>

        <!-- nav -->

        <nav class="nav-menu">
            <ul class="nav-list">
                {
                    data?.links.map((link: any) => (
                        <li>
                            <a href={link.href}>
                                {link.label}

                                {link.badge && (
                                    <span class="nav-badge">{link.badge}</span>
                                )}
                            </a>
                        </li>
                    ))
                }
            </ul>
        </nav>

        <!-- button -->

        <div class="nav-actions">
            <a href={data?.button.href} class="btn-primary">
                {data?.button.label}
            </a>
        </div>
    </div>
</header>
